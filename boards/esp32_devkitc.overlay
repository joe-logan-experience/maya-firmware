/ {
    leds {
        compatible = "gpio-leds";

        led0: user_led {
            gpios = <&gpio0 2 GPIO_ACTIVE_HIGH>;  /* onboard LED on GPIO2, active-low on many boards */
            label = "User LED";
        };
    };

    aliases {
        led0 = &led0;
    };
};

/* Enable the ESP32 Bluetooth HCI driver */
&esp32_bt_hci {
    status = "okay";
    bt-hci-name = "bt_hci0";
};
